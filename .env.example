# Frontend Configuration
# Vite exposes variables that start with VITE_ to the client bundle
VITE_API_URL=https://blockchain-audit-api.onrender.com
VITE_BLOCKCHAIN_API=https://blockchain-audit-api.onrender.com/api

# Local Development
# For local dev, the vite server proxies `/api` to the backend (see vite.config.js)
# VITE_API_URL=http://localhost:5000

# Biometric Backend
EXPOSE_CONFLICT_FIELD=false

# Blockchain Configuration (for local testing)
# BLOCKCHAIN_RPC_URL=http://localhost:8545
# BLOCKCHAIN_CONTRACT_ADDRESS=0x...
# BLOCKCHAIN_PRIVATE_KEY=0x...

# Notes:
# - Update VITE_API_URL to your Render deployment URL
# - For Render deployment, set VITE_BLOCKCHAIN_API to your Render service URL
# - BLOCKCHAIN_RPC_URL and CONTRACT_ADDRESS are used by the API server
