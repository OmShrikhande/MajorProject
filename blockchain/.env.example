# Blockchain API Server Configuration

# Server Port (default: 5000)
PORT=5000

# Environment
NODE_ENV=production

# Blockchain RPC Configuration
# For Sepolia testnet (recommended)
BLOCKCHAIN_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Smart Contract Address (get this after deployment on Sepolia)
BLOCKCHAIN_CONTRACT_ADDRESS=0x...

# Private Key for signing transactions (REQUIRED for write operations)
# This account must have ETH for gas fees on Sepolia
BLOCKCHAIN_PRIVATE_KEY=0x...

# Frontend URLs for CORS
FRONTEND_URL=https://majorpr.netlify.app,http://localhost:5173

# Notes:
# - BLOCKCHAIN_RPC_URL: Set to Sepolia testnet RPC endpoint
# - Get INFURA_KEY from https://www.infura.io/ (free tier available)
# - BLOCKCHAIN_CONTRACT_ADDRESS: Deployed BiometricAuditLog contract on Sepolia
# - BLOCKCHAIN_PRIVATE_KEY: Private key of account that will anchor events
#   * This account needs ETH for gas fees (get from Sepolia faucet)
#   * Use an account specifically created for this purpose
#   * NEVER share this key, keep it secure in production
# - FRONTEND_URL: URLs allowed to access this API (for CORS)
