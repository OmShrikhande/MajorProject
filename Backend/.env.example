FLASK_ENV=production
SECRET_KEY=your-secure-random-secret-key-here
JWT_SECRET=your-secure-random-jwt-secret-here

DATABASE_URL=postgresql://user:password@host:5432/database
PORT=5000

FRONTEND_URL=https://majorpr.netlify.app
ALLOWED_ORIGINS=https://majorpr.netlify.app,https://www.majorpr.netlify.app,http://localhost:5173,http://localhost:3000

BLOCKCHAIN_API_URL=https://majorproject-cjno.onrender.com
EXPOSE_CONFLICT_FIELD=false

MAX_CONTENT_LENGTH=52428800
REDIS_URL=redis://default:password@hostname:6379/0

# DEPLOYMENT NOTES:
# ==================
# 1. BLOCKCHAIN_API_URL: URL of the Node.js blockchain server (same as frontend VITE_BLOCKCHAIN_API)
# 2. This allows the backend to anchor events to the blockchain when users authenticate
# 3. Make sure both backend and blockchain servers are running on Render
# 4. DATABASE_URL: PostgreSQL connection string - get from Render database settings
# 5. Generate new SECRET_KEY and JWT_SECRET for production
